/** @type {import('next').NextConfig} */
const nextConfig = {
  // В Next.js 14 appDir включен по умолчанию
  experimental: {
  },
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig